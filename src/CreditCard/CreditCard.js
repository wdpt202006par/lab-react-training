import React from 'react';

const CreditCard = props => {
    let divStyle = {
        color: {props.color},
        backgroundColor: {props.bgColor};

    }
    return(

    )
}

export default CreditCard;